import topics from './topics';
import users from './users';
import devices from './devices';
import history from './history';

export { topics, users, devices, history };

export default {
  topics,
  users,
  devices,
  history
};
